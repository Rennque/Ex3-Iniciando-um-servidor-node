# Ex3-Iniciando-um-servidor-node

Instructions: 1-> Type the command (npm install) in the terminal to download the dependencies that are in package.json

2-> Type the command (npm start) to start the server